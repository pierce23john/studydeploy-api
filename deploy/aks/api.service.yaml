apiVersion: v1
kind: Service
metadata:
  name: studydeploy-api
  namespace: studydeploy
spec:
  type: LoadBalancer
  selector:
    app: studydeploy-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
